public interface Hewan {
    String berjalan();

    int kaki();

    /*
    Interface merupakan versi full dari abstract class, jadi semua methodnya
    harus dioveride dan interface tidak dapat digunakan sebagai instansi object.
     */
}
